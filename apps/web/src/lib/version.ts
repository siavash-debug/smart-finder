/**
 * Reported by `/healthz`. Kept in sync with `apps/web/package.json` by hand — the manifest is
 * not guaranteed to be readable from the standalone Next.js server output.
 */
export const WEB_VERSION = "0.1.0";
